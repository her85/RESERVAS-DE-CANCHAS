<!DOCTYPE html>
<html>
<head>
  <title>Reservas</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Mis Reservas</h1>
  <ul>
    <% reservas.forEach(reserva => { %>
      <li>
        <h3><%= reserva.cancha && reserva.cancha.nombre ? reserva.cancha.nombre : reserva.cancha %></h3>
        <p>Fecha: <%= reserva.fecha %></p>
        <p>Hora: <%= reserva.hora %></p>
        <form action="/reservas/eliminar/<%= reserva._id %>" method="POST" style="display:inline;">
          <button type="submit">Eliminar</button>
        </form>
        <form action="/reservas/editar/<%= reserva._id %>" method="POST" style="display:inline;">
          <input type="text" name="cancha" value="<%= reserva.cancha && reserva.cancha.nombre ? reserva.cancha.nombre : reserva.cancha %>" required>
          <input type="text" name="usuario" value="<%= reserva.usuario %>" required>
          <input type="date" name="fecha" value="<%= reserva.fecha %>" required>
          <input type="time" name="hora" value="<%= reserva.hora %>" required>
          <button type="submit">Editar</button>
        </form>
      </li>
    <% }); %>
  </ul>
  <a href="/">Volver al inicio</a>
</body>
</html>